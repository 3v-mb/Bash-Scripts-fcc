(( ... )) will perform a calculation or operation and output nothing. $(( ... )) will replace the calculation with the result of it. 
Use <<, >>, <=, >= for comparisons here - these braces can also be used in if conditions

[[ ]] - used in loops or conditions for conditional expressions
-le, -ge, -gt, -lt, 

- notes for shell scripting
